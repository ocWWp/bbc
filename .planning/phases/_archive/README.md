# `.planning/phases/_archive/`

Alternate / draft phase plans from earlier sessions. Preserved for audit, **not canonical**.

The canonical phase set is the tracked top-level folders under `.planning/phases/` (F0–F5, Y-missing-pieces). The roadmap at `.planning/ROADMAP.md` references only those.

If you find content here that should be canonical, propose a promotion via `scripts/propose.sh --target main --kind add`.
