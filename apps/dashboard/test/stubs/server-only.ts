// No-op stub for Next.js "server-only" marker in test runs. The real package
// throws if bundled into a client-side build; in tests there is no client,
// so we just provide an empty module.
export {};
