{
  "name": "@bbc/mcp-server",
  "version": "0.1.0",
  "private": true,
  "description": "BBC MCP server — Model Context Protocol bridge so agents can read+write a tenant's BBC. Per-tenant API keys, RLS-gated under the hood.",
  "license": "AGPL-3.0",
  "type": "module",
  "main": "./src/server.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "type-check": "tsc --noEmit",
    "dev": "node --import tsx/esm src/server.ts"
  },
  "dependencies": {
    "@bbc/store": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@supabase/supabase-js": "^2.106.0"
  },
  "devDependencies": {
    "@types/node": "^22.19.19",
    "tsx": "^4.22.4",
    "typescript": "^5.6.0"
  }
}
