<!-- BBC-AUTO-HEADER:BEGIN -->
> **Auto-generated by `bootstrap-leaf.sh`. Do not edit between the BEGIN/END markers.**
> The two sections below ("Precedence rule" and "Non-negotiable principles") are extracted **verbatim** from `bbc/CLAUDE.md`. They are NOT the entirety of Main — for the lock matrix, "What changes this file," and "What this file does NOT decide," read `../../CLAUDE.md` directly.
> Re-run `bash ../../scripts/bootstrap-leaf.sh <this-leaf>` to refresh after Main changes.

# Inherited from Main (`bbc/CLAUDE.md` — verbatim)

## Precedence rule

```
Main (this file) > Manager (bbc/manager/CLAUDE.md) > Distribution (bbc/distribution/<leaf>/CLAUDE.md)
```

A lower-layer document can **specialize** an upper rule (add detail, scope to a subset). It cannot **override**, **weaken**, or **contradict** an upper rule. If a conflict arises, Main wins; the agent flags the conflict and stops the action.

## Non-negotiable principles

1. **Memory is the contract.** All durable knowledge lives in `memory/` as Markdown + YAML frontmatter (schema in `memory/_schema.md`). If a fact isn't in memory, it isn't real.
2. **Direct writes are scoped to your `owning_layer`.** Anything else goes through the queue.
3. **Proposals are append-only; resolutions move (not delete).** Accepted proposals land in `queue/_accepted/`, rejected in `queue/_rejected/`. Both stay forever — they are the audit trail.
4. **Vendor names are not architecture.** Roles (`llm-provider`, `db-provider`, `email-delivery`) live in `memory/ops/vendors.md`. Any other file that needs to mention a vendor cites that file. This protects us from vendor churn.
5. **Voice is single-source.** `memory/design/voice-tone.md` is canonical. The cross-repo voice anchors (`pillar-interactions.ts`, `prompts.py`) are downstream consumers.
6. **No silent autonomy.** V1 has no daemons, no background agents, no auto-accept. Every state change is either a human edit at the layer that owns the file, or a queued proposal that passes through `accept.sh` / `reject.sh`.

# How to act as a Distribution leaf

Read `../../manager/CLAUDE.md` for the review protocol. To propose any change to Main- or Manager-owned files, run from inside this leaf directory:

```bash
bash ../../scripts/propose.sh \
  --target <main|manager> \
  --file <path-from-repo-root> \
  --kind <edit|add|supersede> \
  --summary "<short>" \
  [--source "<who/what said so>"] \
  [--body-file <path-with-diff-or-new-content>]
```

Manager reviews via `manager/agents/queue-reviewer.md`. Approved proposals are applied by a human at Main running `scripts/accept.sh`.
<!-- BBC-AUTO-HEADER:END -->

# CLAUDE.md — Leaf: 8azi-web

You are operating as the **8azi-web** Distribution leaf. The auto-header above is inherited verbatim from Main; everything below is leaf-local.

## What this leaf governs

This leaf shadows the real Next.js frontend repo at:

```
/Users/grid/Documents/GitHub/8azi-web/
```

That repo has its own `CLAUDE.md`, `AGENTS.md`, and `.claude/` agents — those continue to live in-repo and remain the source of truth for the *codebase's* day-to-day. **This file is the BBC-side governance**: how this leaf participates in the queue protocol, which skill specializations apply, and which cross-repo invariants this leaf is responsible for.

## Skill specialization

When invoking review skills from this leaf, the resolver picks `8azi-web.pr-review` (declared at `memory/skills/8azi-web/pr-review.yaml`, extends `general.pr-review`). Specific rules added by this leaf:

- Tailwind class ordering per `prettier-plugin-tailwindcss`.
- New direct vendor SDK use must include a `// bbc-provider:<id>` comment at the call site (F4-build-2 convention).
- Cross-repo voice anchor sync (`pillar-interactions.ts` ↔ `prompts.py` in 8azi-api) must remain consistent.
- New vendor names in prose are rejected per `manager/rules/no-vendor-names-in-prose.md`.

To invoke the resolved skill: `/bbc:invoke pr-review` from inside this leaf.

## Cross-repo invariants this leaf enforces

| Invariant | Files | Test |
|---|---|---|
| Voice anchor | `8azi-web/src/shared/lib/voice/pillar-interactions.ts` ↔ `8azi-api/app/shared/llm/prompts.py` ↔ `8azi-api/app/features/party/router.py` | Manual same-week propagation per `manager/rules/cross-leaf-sync.md` |
| Nayin lookup | `8azi-web/src/shared/data/generated/nayin-lookup.json` (generated from `8azi-api/app/bazi/constants.py`) | `src/shared/data/__tests__/nayin-cross-repo-sync.test.ts` enforces |

When either invariant breaks, file a queue proposal (cross_leaf_impact: yes) so Manager can coordinate the fix across both repos.

## Local-only knowledge

`local/` is for pre-promotion notes specific to 8azi-web — observations that haven't earned an org-wide promotion yet (e.g., "the cf:deploy script needs an extra warmup on cold builds"). Promote up via `bash ../../scripts/propose.sh --target main --kind add ...` once the observation is org-relevant.

## Tagged vendor callsites in this leaf's repo

Per F4-build-2, the following files in `/Users/grid/Documents/GitHub/8azi-web/` carry `bbc-provider:<id>` tags identifying vendor SDK use:

- `src/shared/lib/supabase/{service,server,browser}.ts` → `bbc-provider:supabase`
- `src/app/auth/confirm/page.tsx` → `bbc-provider:supabase`
- `wrangler.toml` → `bbc-provider:cloudflare-workers`
- `open-next.config.ts` → `bbc-provider:cloudflare-workers`

Decommissioning either provider would grep for these tags and require this leaf to replace them. See `/bbc:decommission` runbook.

## Dev workflow (leaf-local; not BBC-governed)

For day-to-day development conventions, branch strategy, deploy, etc., read `/Users/grid/Documents/GitHub/8azi-web/CLAUDE.md` directly — it has the canonical commands (`npm run dev`, `cf:deploy`, etc.), branch model (dev → main), and security audit list.

This BBC-side leaf file does NOT duplicate that content. It governs only the BBC-protocol participation.

## Hard rules (cannot be specialized away)

1. Cannot edit anything outside `bbc/distribution/8azi-web/`. To change shared memory, file a proposal via `propose.sh`.
2. Cannot contradict Main or Manager rules. If an exception is needed, propose a Manager rule change.
3. The voice anchor and bindings are read-only to this leaf. Cite them; do not duplicate.
4. New direct vendor SDK use in `8azi-web/` requires a `bbc-provider:<id>` tag at the call site.

## Reading checklist for every fresh session

- [ ] `../../CLAUDE.md` — Main precedence rule + lock matrix
- [ ] `../../manager/CLAUDE.md` — review protocol you'll trigger when you propose
- [ ] This file — leaf rules
- [ ] `local/` — anything pending here
- [ ] `../../memory/_index.md` — what's known org-wide

## What goes here (specialize as needed)

When you fill out a real leaf, replace the rest of this file with:

- The repo path this leaf shadows (e.g., `/Users/grid/Documents/GitHub/8azi-web/`).
- Leaf-specific commands and how to run them.
- Leaf-specific tooling (e.g., MCP servers used here, dev/test commands).
- File-ownership rules within this repo (high caution / touch sparingly / free game) — see `8azi-web/AGENTS.md` for the pattern.

Until then: this leaf is a stub. It exists to prove bootstrap works, not to do work.
