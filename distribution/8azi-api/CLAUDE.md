<!-- BBC-AUTO-HEADER:BEGIN -->
> **Auto-generated by `bootstrap-leaf.sh`. Do not edit between the BEGIN/END markers.**
> The two sections below ("Precedence rule" and "Non-negotiable principles") are extracted **verbatim** from `bbc/CLAUDE.md`. They are NOT the entirety of Main — for the lock matrix, "What changes this file," and "What this file does NOT decide," read `../../CLAUDE.md` directly.
> Re-run `bash ../../scripts/bootstrap-leaf.sh <this-leaf>` to refresh after Main changes.

# Inherited from Main (`bbc/CLAUDE.md` — verbatim)

## Precedence rule

```
Main (this file) > Manager (bbc/manager/CLAUDE.md) > Distribution (bbc/distribution/<leaf>/CLAUDE.md)
```

A lower-layer document can **specialize** an upper rule (add detail, scope to a subset). It cannot **override**, **weaken**, or **contradict** an upper rule. If a conflict arises, Main wins; the agent flags the conflict and stops the action.

## Non-negotiable principles

1. **Memory is the contract.** All durable knowledge lives in `memory/` as Markdown + YAML frontmatter (schema in `memory/_schema.md`). If a fact isn't in memory, it isn't real.
2. **Direct writes are scoped to your `owning_layer`.** Anything else goes through the queue.
3. **Proposals are append-only; resolutions move (not delete).** Accepted proposals land in `queue/_accepted/`, rejected in `queue/_rejected/`. Both stay forever — they are the audit trail.
4. **Vendor names are not architecture.** Roles (`llm-provider`, `db-provider`, `email-delivery`) live in `memory/ops/vendors.md`. Any other file that needs to mention a vendor cites that file. This protects us from vendor churn.
5. **Voice is single-source.** `memory/design/voice-tone.md` is canonical. The cross-repo voice anchors (`pillar-interactions.ts`, `prompts.py`) are downstream consumers.
6. **No silent autonomy.** V1 has no daemons, no background agents, no auto-accept. Every state change is either a human edit at the layer that owns the file, or a queued proposal that passes through `accept.sh` / `reject.sh`.

# How to act as a Distribution leaf

Read `../../manager/CLAUDE.md` for the review protocol. To propose any change to Main- or Manager-owned files, run from inside this leaf directory:

```bash
bash ../../scripts/propose.sh \
  --target <main|manager> \
  --file <path-from-repo-root> \
  --kind <edit|add|supersede> \
  --summary "<short>" \
  [--source "<who/what said so>"] \
  [--body-file <path-with-diff-or-new-content>]
```

Manager reviews via `manager/agents/queue-reviewer.md`. Approved proposals are applied by a human at Main running `scripts/accept.sh`.
<!-- BBC-AUTO-HEADER:END -->

# CLAUDE.md — Leaf: 8azi-api

You are operating as the **8azi-api** Distribution leaf. The auto-header above is inherited verbatim from Main; everything below is leaf-local.

## What this leaf governs

This leaf shadows the FastAPI backend repo at:

```
/Users/grid/Documents/GitHub/8azi-api/
```

That repo has its own `README.md` and source — those continue to be canonical for codebase concerns. **This file is the BBC-side governance**: queue-protocol participation, skill specialization, and the cross-repo invariants this leaf enforces.

## Skill specialization

When invoking review skills from this leaf, the resolver picks `8azi-api.pr-review` (declared at `memory/skills/8azi-api/pr-review.yaml`, extends `general.pr-review`). Specific rules added by this leaf:

- Reject any PR that exposes service-role keys to client code paths.
- Reject PRs adding new endpoints without rate-limit middleware.
- Reject PRs adding new Pydantic models without input validation.
- Verify Nayin lookup sync is intact (cross-repo test passes).
- Reject if PR adds direct vendor SDK use without `bbc-provider:<id>` tag at the call site.

To invoke the resolved skill: `/bbc:invoke pr-review` from inside this leaf.

The default verdict bias for auth/RLS/secret-related changes is `changes_requested` until a maintainer signs off.

## Cross-repo invariants this leaf enforces

| Invariant | Files | Test |
|---|---|---|
| Voice anchor | `8azi-api/app/shared/llm/prompts.py` ↔ `8azi-api/app/features/party/router.py` ↔ `8azi-web/src/shared/lib/voice/pillar-interactions.ts` | Manual same-week propagation per `manager/rules/cross-leaf-sync.md` |
| Nayin lookup | `8azi-api/app/bazi/constants.py` is the **canonical source**. `8azi-web/src/shared/data/generated/nayin-lookup.json` is generated from it via `python scripts/regenerate-nayin-lookup.py`. | `8azi-web/src/shared/data/__tests__/nayin-cross-repo-sync.test.ts` enforces |

This leaf is the **upstream** of both invariants — changes here propagate to the web leaf, not the other way around. When voice rules or Nayin constants change, file a queue proposal with `cross_leaf_impact:` listing 8azi-web as affected.

## Tagged vendor callsites in this leaf's repo

Per F4-build-2, the following files in `/Users/grid/Documents/GitHub/8azi-api/` carry `bbc-provider:<id>` tags identifying vendor SDK use:

- `app/services/ai.py` → `bbc-provider:anthropic-claude-sonnet`
- `app/routers/party.py` → `bbc-provider:anthropic-claude-sonnet`
- `app/services/supabase.py` → `bbc-provider:supabase`
- `app/services/email.py` → `bbc-provider:resend`

Decommissioning any of these vendors triggers a Quarantine sweep that greps for these tags and requires this leaf to replace them. See `/bbc:decommission` runbook.

## Dev workflow (leaf-local; not BBC-governed)

For day-to-day development conventions, deploy commands, and security audit list, read `/Users/grid/Documents/GitHub/8azi-api/README.md` directly. This BBC-side leaf file does NOT duplicate that content.

Key commands (per the README):
- `pytest` — run tests
- `mypy app` — type-check
- Deploy via `8azi/.github/workflows/deploy.yml` → Railway

## Hard rules (cannot be specialized away)

1. Cannot edit anything outside `bbc/distribution/8azi-api/`. To change shared memory, file a proposal via `propose.sh`.
2. Cannot contradict Main or Manager rules. If an exception is needed, propose a Manager rule change.
3. The voice anchor and bindings are read-only to this leaf.
4. New direct vendor SDK use in `8azi-api/` requires a `bbc-provider:<id>` tag at the call site (F4-build-2 convention).
5. **Backend security floor**: any PR touching auth, RLS policies, or secret handling defaults to `changes_requested` until a maintainer explicitly signs off. This is stricter than `general.pr-review` and is the leaf's added rule (see Skill specialization above).
