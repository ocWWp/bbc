# CLAUDE.md — Distribution leaf (template)

> **This is a template.** When you copy it for a real leaf via `scripts/bootstrap-leaf.sh <name>`, the bootstrap script prepends Main + Manager headers above this content. Do not edit those headers — they are regenerated by re-running bootstrap.

## You are operating at the Distribution layer

Read these in order before doing anything:

1. `../../CLAUDE.md` (Main)
2. `../../manager/CLAUDE.md` (Manager)
3. This file

If `bootstrap-leaf.sh` was run, the relevant Main + Manager content is already inlined in this file's header section — but always verify against the source files in case bootstrap is stale.

## Your scope

You are a **leaf**: a single workstream's view into BBC. You own:

- Your own `local/` directory — for pre-promotion notes, drafts, and leaf-only knowledge.
- Your own `commands/` directory — for leaf-specific slash commands or scripts.
- This `CLAUDE.md` — leaf-local rules that specialize Main + Manager rules.

## Hard rules (cannot be specialized away)

1. You cannot edit anything outside `distribution/<your-leaf>/`. To change shared memory, file a proposal: `bash ../../scripts/propose.sh --target <main|manager> --file <path> --kind <edit|add|supersede> --summary "<short>"`.
2. Your leaf's rules **cannot contradict** Main or Manager rules. If you need an exception, propose a Manager rule change (`--target manager`) that explicitly carves out the exception. Do not silently override.
3. The voice anchor (`memory/design/voice-tone.md`) and vendor table (`memory/ops/vendors.md`) are read-only to you. Cite them; do not duplicate.

## Reading checklist for every fresh session

- [ ] `../../CLAUDE.md` — Main precedence rule + lock matrix
- [ ] `../../manager/CLAUDE.md` — review protocol you'll trigger when you propose
- [ ] This file — leaf rules
- [ ] `local/` — anything pending here
- [ ] `../../memory/_index.md` — what's known org-wide

## What goes here (specialize as needed)

When you fill out a real leaf, replace the rest of this file with:

- The repo path this leaf shadows (e.g., `/Users/grid/Documents/GitHub/8azi-web/`).
- Leaf-specific commands and how to run them.
- Leaf-specific tooling (e.g., MCP servers used here, dev/test commands).
- File-ownership rules within this repo (high caution / touch sparingly / free game) — see `8azi-web/AGENTS.md` for the pattern.

Until then: this leaf is a stub. It exists to prove bootstrap works, not to do work.
