---
id: provider_railway
provider_id: railway
type: provider-adapter
implements: [api-host]
contract_version: 1
status: active
scope: org
layer: main
owning_layer: main
created: 2026-05-08T00:00:00Z
updated: 2026-05-08T00:00:00Z
tags: [adapter, hosting, railway]
---

# Adapter: railway

## Metadata

- container_runtime: docker
- supports_horizontal_scaling: true              # via replicas; verify pricing tier
- max_request_duration_seconds: 600              # default; configurable
- regions: [us-west-1, us-east-1, eu-west-1]     # verify project location
- price_model: usage-based (cpu-seconds + memory-gb-hours)

## Runtime

- deploy: triggered by git push to main (`8azi/.github/workflows/deploy.yml`)
- container source: 8azi-api/Dockerfile
- auth: Railway API token in GitHub Actions
- env vars: managed in Railway dashboard

## bbc-provider-tag

```
bbc-provider:railway
```

## Stability signals

- vendor_status_page: https://status.railway.com/
- last_incident_seen: <unknown — start tracking 2026-05-08>
- declared_uptime_sla: not formally published

## Notes

8azi-api deploys here. Container build is reproducible from `8azi-api/Dockerfile`. Logs accessible via Railway dashboard or `railway logs` CLI.
