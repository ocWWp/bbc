---
type: skill
contract_version: 1
scope: leaf:8azi-web
layer: distribution
owning_layer: distribution
created: 2026-05-08T00:00:00Z
updated: 2026-05-08T00:00:00Z
status: accepted
tags: [skill, review, web, frontend]
id: skill_8azi-web-pr-review
skill_id: 8azi-web.pr-review
resolved_for_caller: 8azi-web
---

# Resolution trace
resolution_trace:
  requested: pr-review
  caller: 8azi-web
  chain:
    - { skill_id: skill, path: memory/skills/_abstract/skill.yaml }
    - { skill_id: review-skill, path: memory/skills/_abstract/review-skill.yaml }
    - { skill_id: general.pr-review, path: memory/skills/general/pr-review.yaml }
    - { skill_id: 8azi-web.pr-review, path: memory/skills/8azi-web/pr-review.yaml }
  effective_skill_id: 8azi-web.pr-review

# === inherited from skill (memory/skills/_abstract/skill.yaml) ===
# Abstract: skill (root)

The root abstract base. All skills in the BBC tree extend this directly or transitively.

## Required fields (every concrete skill must declare)

- skill_id: dotted notation (e.g., `general.pr-review`, `marketing.pr-review`)
- inputs: typed schema
- outputs: typed schema
- preconditions: list of predicates
- allowed_tools: list of tool names

## Required behavior

- `invoke(input) -> output` — abstract; concrete skills must define.
- `validate(input) -> bool` — derived from inputs schema.
- `preconditions()` — must return true before invoke.

## Notes

This file is the entry point for the resolver. Any skill that doesn't trace to `skill` via `extends:` is invalid and rejected by `validate-skill-tree.sh`.

# === inherited from review-skill (memory/skills/_abstract/review-skill.yaml) ===
# Abstract: review-skill

Base for any skill that reviews something (PRs, code, docs, designs) and produces a verdict.

## Inputs

- target: { type: file_path | pr_url | text-block }
- context: { type: object, optional: true }

## Outputs

- verdict: { type: enum, values: [approved, changes_requested, rejected] }
- rationale: { type: string, max_length: 2000 }
- findings: { type: list, item_type: finding }

## Preconditions

- target must exist or be reachable.
- caller must have read access to target.

## Behavior contract

A review-skill produces a verdict + rationale + findings. It does NOT mutate the target. Concrete subclasses define the specific rules used to derive the verdict.

# === inherited from general.pr-review (memory/skills/general/pr-review.yaml) ===
# Skill: general.pr-review

Base concrete reviewer for any pull request, regardless of leaf. Extended by leaf-specific specializations (e.g., marketing.pr-review, 8azi-web.pr-review).

## Inherits

- inputs: from review-skill
- outputs: from review-skill
- preconditions: from review-skill, with additions below.

## Adds

rules:
  - "Cite a file:line for every finding."
  - "Reject if any finding is severity: critical."
  - "Approve only if all severity: high findings have a maintainer signoff."
  - "Verify cross-repo sync invariants where applicable (Nayin lookup, voice anchors)."
voice: terse-engineering
allowed_tools: [Read, Grep, Glob, Bash]

## Body

When given a PR (URL or branch + base):
1. Read the diff.
2. For each modified file, evaluate against rules above.
3. Surface findings as structured list with severity.
4. Compute verdict: rejected if any critical; changes_requested if high without signoff; approved otherwise.
5. Render rationale in voice: terse-engineering — facts, no hedging.

# === inherited from 8azi-web.pr-review (memory/skills/8azi-web/pr-review.yaml) ===
# Skill: 8azi-web.pr-review

Inherits from `general.pr-review`. Adds web-specific rules + reuses engineering voice.

## Inherits

- inputs, outputs, preconditions, voice: from general.pr-review (terse-engineering)

## Overrides

rules:
  add:
    - "Tailwind classes ordered per the prettier-plugin-tailwindcss spec"
    - "Reject if PR adds direct vendor SDK use without bbc-provider:<id> tag at the call site"
    - "Verify cross-repo voice anchor sync (pillar-interactions.ts ↔ prompts.py)"
    - "Reject if PR introduces vendor name in prose outside allowed locations (per manager/rules/no-vendor-names-in-prose.md)"
  keep_from_parent: true

## Body addendum

When reviewing 8azi-web PRs, also check:
1. Tailwind class ordering in modified TSX files.
2. Any new direct import of a third-party SDK has a `// bbc-provider:<id>` comment at the call site (F4-build-2 convention).
3. Cross-repo voice anchor file (`pillar-interactions.ts`) hasn't drifted from `prompts.py`.
