---
id: mem_2026-05-08_tech-repo-structure
type: fact
scope: org
layer: main
source: human:zeth
created: 2026-05-08T00:00:00Z
updated: 2026-05-08T00:00:00Z
owning_layer: main
tags: [repo, monorepo, structure, layout, bbc-dashboard, mcp-server]
status: accepted
---

# Repo Structure: BBC product code lives in one monorepo

Companion to ADR-0004. Concrete layout for productizing BBC.

## Decision

**Single repo: `github.com/ocWWp/bbc`.** All product code (dashboard, MCP server, protocol docs, templates, deployment artifacts) lives here. No separate `bbc-dashboard` or `bbc-mcp` repos.

**The current `bbc-dashboard` repo is generalized into `apps/dashboard/` inside the monorepo**. The tenant-specific seed data (memory layout, vendors list, voice anchors) stays as one example tenant under `examples/example-tenant/` for demos and onboarding flows.

## Rationale

1. **Solo dev efficiency.** One PR can touch protocol + dashboard + MCP server when a feature requires it. No coordinated multi-repo releases.
2. **Two-mode parity is enforceable.** The storage interface (`MemoryStore`, etc.) lives once in `packages/store/`; both dashboard and MCP server import it. Drift is impossible by construction.
3. **One CI, one issue tracker, one releases page.** Less ceremony.
4. **OSS visibility.** A self-hoster cloning the AGPL repo gets the entire product in one `git clone`. `docker compose up` in the repo root just works.

The cost — every contributor pulls the entire tree even if they only care about the protocol docs — is small for a project this size and acceptable for an open-core tool with a tight surface.

## Layout

```
bbc/
├── README.md                          # Product README (protocol + how to run + self-host)
├── LICENSE                            # AGPL-3.0
├── CLAUDE.md                          # Main precedence rules, principles, lock matrix
├── AGENTS.md                          # Onboarding cheat sheet
├── docker-compose.yml                 # Self-host quickstart (Phase 7)
├── package.json                       # Workspace root (pnpm or npm workspaces)
├── pnpm-workspace.yaml                # Workspace manifest
│
├── memory/                            # Protocol memory (current state, unchanged)
│   ├── _schema.md
│   ├── _index.md                      # Auto-generated by scripts/index-memory.sh
│   ├── decisions/                     # ADRs
│   ├── product/
│   ├── design/
│   ├── tech/
│   ├── ops/
│   ├── people/
│   └── glossary/
│
├── manager/                           # Manager layer (current state, unchanged)
│   ├── CLAUDE.md
│   ├── agents/
│   └── rules/
│
├── distribution/                      # Distribution leaves (current state, unchanged)
│   ├── _template/
│   ├── <<tenant-app-api>>/                      # Tenant-specific leaf for the tenant's product
│   ├── <<tenant-app-web>>/
│   ├── bbc-dashboard/                # NOTE: stays as a leaf shadowing the app
│   └── dashboard/                     # NOTE: stays as the BBC-product-dashboard leaf
│
├── queue/                             # Proposal queue (file-mode, unchanged)
├── _log/                              # Operations log (file-mode, unchanged)
├── scripts/                           # Bash protocol scripts (file-mode transport, unchanged)
│
├── apps/
│   ├── dashboard/                     # The BBC dashboard (Next.js) — generalized from bbc-dashboard
│   │   ├── src/
│   │   │   ├── app/                   # Next.js app router
│   │   │   ├── components/
│   │   │   └── lib/
│   │   │       ├── auth/              # require-user, allowlist (current session)
│   │   │       ├── supabase/          # @supabase/ssr clients (current session)
│   │   │       └── store/             # NEW: MemoryStore/QueueStore/LogStore
│   │   │           ├── interfaces.ts
│   │   │           ├── local/         # File-mode impl (Phase 7)
│   │   │           └── supabase/      # DB-mode impl (Phase 2)
│   │   ├── supabase/migrations/       # SQL migrations (current session has 0001+0002; Phase 1+ adds more)
│   │   ├── package.json
│   │   └── README.md
│   │
│   └── mcp-server/                    # NEW: MCP server (Phase 6)
│       ├── src/
│       │   ├── tools/                 # read_memory, list_queue, propose_change, etc.
│       │   ├── auth.ts                # API key resolution -> tenant scope
│       │   └── server.ts              # MCP protocol handler
│       ├── package.json
│       └── README.md
│
├── packages/
│   ├── store/                         # NEW: shared storage interfaces + implementations
│   │   ├── src/
│   │   │   ├── interfaces.ts          # MemoryStore, QueueStore, LogStore types
│   │   │   ├── local/                 # File-mode impl
│   │   │   └── supabase/              # DB-mode impl
│   │   └── package.json
│   │
│   ├── schema/                        # NEW: shared schema types (memory frontmatter, queue frontmatter)
│   │   ├── src/
│   │   └── package.json
│   │
│   └── protocol/                      # NEW: shared protocol primitives (actor strings, hashing, validators)
│       ├── src/
│       └── package.json
│
├── templates/                         # NEW: seed templates for new tenants (Phase 4)
│   └── initial-tenant/
│       ├── memory/
│       │   ├── CLAUDE.md.tpl
│       │   └── _schema.md
│       ├── manager/
│       │   └── CLAUDE.md.tpl
│       ├── bindings.yaml.tpl
│       └── README.tpl
│
└── examples/                          # NEW: example tenants (read-only, for demos and tests)
    └── example-tenant/                   # The current BBC tenant memory layout, lifted as an example
        ├── memory/
        ├── distribution/
        └── README.md                  # "This is one example of how to use BBC, not BBC itself"
```

## Migration plan (bbc-dashboard → apps/dashboard/)

This happens as the first task of Phase 1, not Phase 0. Phase 0 just records the decision.

1. **Create `apps/dashboard/`** in the monorepo. Copy the contents of `/Users/grid/Documents/GitHub/bbc-dashboard/` into it (preserving git history via `git subtree add` if reasonable; otherwise a flat copy with a single migration commit).
2. **Update import paths.** Most code uses `@/*` aliases (already in `tsconfig.json`); those keep working. Anything referencing the BBC repo via `BBC_REPO=../bbc` env var changes to relative paths inside the monorepo (or stays env-var-driven so file-mode self-host still works).
3. **Wire workspaces.** Root `package.json` declares workspaces; `apps/dashboard/package.json` becomes a workspace member. Same for any future `apps/mcp-server/` and `packages/*`.
4. **Update `bbc/distribution/dashboard/CLAUDE.md`.** The leaf currently shadows an external `bbc-dashboard` repo. After the move, it shadows `apps/dashboard/` in the same repo. Update the `What this leaf governs` section and the `Quick map` paths.
5. **Archive the originally-standalone dashboard repo.** Add a README pointing to `github.com/ocWWp/bbc/apps/dashboard/`. Don't delete it (broken links to old commits matter).
6. **Update `.env.local` and `bbc/.claude/commands/bbc/dashboard.md`** to point at the new in-monorepo path. The `/bbc:dashboard` skill we updated in this session needs its hardcoded `/Users/grid/Documents/GitHub/bbc-dashboard` paths swapped for `apps/dashboard/`.

## Workspace tooling

**pnpm workspaces** is the recommended choice — better than npm workspaces for monorepo dependency hoisting, faster installs, and Vercel/Railway/Fly all handle pnpm natively.

The root `package.json` declares:
```json
{
  "name": "bbc-monorepo",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev:dashboard": "pnpm --filter @bbc/dashboard dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  }
}
```

Optional later: Turborepo or Nx for build caching once the tree gets bigger. Not needed for v1.

## What stays separate

- **`<<<tenant-app-web>>>` / `<<<tenant-app-api>>>`** stay as separate repos. They are *applications using BBC*, not parts of the BBC product. The `distribution/<<tenant-app-web>>/` and `distribution/<<tenant-app-api>>/` leaves inside the BBC monorepo continue to govern those external repos via the existing leaf protocol.

- **Per-tenant deployments** in production are not in this repo. Each customer's BBC data lives in their tenant rows on the SaaS Postgres (DB-mode) or in their own self-host (file-mode). The repo holds the product code, not customer data.

## Out of scope for this doc

- Workspace tooling configuration files (Phase 1 task).
- TypeScript path aliases between packages (Phase 1).
- CI/CD wiring (Phase 9).
- Specific `package.json` content per package (Phase 1+).
